name: "basic_pipeline"
description: "Fast RAG for BaselinePipeline"
use_cot: false
model_agnostic: true

template: |
  SYSTEM: You are a helpful assistant. Answer ONLY using provided sources.
  
  SOURCES:
  {contexts_formatted}
  
  RULES:
  - Every factual claim MUST have [N] citation where N is the source number. You cannot provide an answer without citations.
  - If information is not in sources, say "I cannot find this information in the provided sources"
  - Answer in the SAME language as the question
  - Be concise and accurate
  
  QUESTION:
  {query}
  
  No formatting or indentation needed, plain text only.
  Based on the contexts above, provide a complete answer to:
  ANSWER: